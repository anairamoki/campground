Editing and Updating campgrounds and saving them into Campground DB

44th - Adding Route to campgrounds folder <edit.ejs> 

copy the new.ejs template and past it into edit.ejs 

45th - in the show.ejs make a new link 

46th - install method-override in the terminal
• stop the server 
• type npm i method-override
• start the server again

47th -require the method-override
48th - use the method-override, and pass the string we want to use in query - which is ('_method')
49th- set the put route
50th - in the edit.ejs add <%=campground._id%>?_method=PUT" in the form tag.
?_method=PUT" query string for methodOverride

the output should look like this:
<form action="/campgrounds/<%=campground._id%>?_method=PUT" method="POST">

51st - 
{ ...req.body.campground } -> this campground comes from edit.ejs on the name value inside of input tag (name="campground[title]")

• the req.body.campground object's properties, which we defined in mongoDB campground model is spread out like below:
• The output look like this:
Like {title:name,

        location:location}

OLD VERSION
<div>
      <label for="title">Title</label>
      <input type="text" id="title" name="campground[title]" value=" <%=campground.title %>">
    </div>
    <div>
      <label for="location">Location</label>
      <input type="text" id="location" name="campground[location]" value="<%= campground.location%>">
    </div>
    <button>Update Campground</button>
  </form> 